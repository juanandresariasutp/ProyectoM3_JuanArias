{
  "name": "vegeta-chat-spa",
  "version": "1.0.0",
  "description": "Vegeta Chat SPA con Google Gemini",
  "type": "module",
  "scripts": {
    "dev": "vite --config vite.config.js",
    "build": "mkdir -p dist && cp -r src/* dist/ && cp -r public/* dist/ 2>/dev/null || true",
    "test": "vitest --config vitest.config.js"
  },
  "devDependencies": {
    "vitest": "^1.0.0",
    "vite": "^5.0.0"
  }
}
